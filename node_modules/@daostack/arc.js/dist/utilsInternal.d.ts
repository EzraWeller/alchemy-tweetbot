import { FilterResult } from "web3";
import { Web3 } from "./utils";
/**
 * Utils not meant to be exported to the public
 */
export declare class UtilsInternal {
    static sleep(milliseconds: number): Promise<any>;
    static ensureArray<T>(arr: Array<T> | T): Array<T>;
    /**
     * Returns the last mined block in the chain.
     */
    static lastBlock(): Promise<number>;
    /**
     * For environments that don't allow synchronous functions
     * @param filter
     */
    static stopWatchingAsync(filter: FilterResult): Promise<any>;
    static getRandomNumber(): number;
    static getWeb3Sync(): Web3;
}
