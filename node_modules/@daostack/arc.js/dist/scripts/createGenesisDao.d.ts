import { Web3 } from "web3";
import { Address } from "../commonTypes";
export interface ForgedDaoInfo {
    avatarAddress: Address;
    orgName: string;
    daoCreatorInst: any;
}
/**
 * Migration callback
 */
export declare class GenesisDaoCreator {
    private web3;
    private network;
    constructor(web3: Web3, network: string);
    /**
     * Forge the Genesis DAO.  Note this does not set the schemes.
     */
    forge(foundersConfigurationLocation: string): Promise<ForgedDaoInfo>;
    setSchemes(forgedDaoInfo: ForgedDaoInfo): Promise<void>;
}
